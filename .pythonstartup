import scipy as sp
import numpy as np
import pandas as pd
import pandas_datareader as pdr
import matplotlib.pyplot as plt

import importlib

import rh
import api
import util
import util.debug
import slurp
import events
import stocks
import models
import account
import constants

from util.numbers import D, Z

def relmod():
    importlib.reload(rh)
    importlib.reload(api)
    importlib.reload(account)
    importlib.reload(stocks)
    importlib.reload(slurp)
    importlib.reload(models)
    importlib.reload(events)
    importlib.reload(util)
    importlib.reload(util.debug)
    importlib.reload(util.color)
    importlib.reload(util.output)

rh.preinitialize()
rh.repl()

datatypes = lambda df=rh.acc._stocks: {
    column: set(map(type, df[column])) for column in df.columns
}

S = rh.acc._stocks
T = rh.acc._transactions
